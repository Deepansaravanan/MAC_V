# Proposed Architecture

The proposed architecture will use configuration registers, a mode controller, a reconfiguration controller, and a MAC datapath. Width, pipeline depth, overflow, and mode-switching semantics remain design decisions to be documented before implementation.

