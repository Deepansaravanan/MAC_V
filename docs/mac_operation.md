# MAC Operation

The future datapath will calculate a signed product and accumulate it with a defined accumulator input. INT8 and INT16 input encoding, result width, rounding, saturation, latency, and handshake behavior must be fixed before RTL implementation.

