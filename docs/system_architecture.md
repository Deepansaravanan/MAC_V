# System Architecture

`Configuration Registers → Mode Controller → Reconfiguration Controller → MAC Datapath → Output`

Control-driven runtime operation is the intended meaning of reconfiguration in this project. FPGA partial reconfiguration is outside the current scope.

